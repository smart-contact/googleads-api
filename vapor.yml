id: 30408
name: googleads-api
environments:
    production:
        memory: 512
        cli-memory: 1024
        runtime: docker
        timeout: 800
        build:
            - 'COMPOSER_MIRROR_PATH_REPOS=1 composer install --no-dev'
            - 'php artisan event:cache'
